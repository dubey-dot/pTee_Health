/**
 * Stand-in until patient selection / routing exists (Phase 3). The
 * Assessment screen is currently a single hardcoded route with no
 * patient picker, so it always points at the one seeded demo patient
 * and assessment on the backend.
 */
export const DEFAULT_PATIENT_ID = "patient-1";
export const DEFAULT_ASSESSMENT_ID = "assessment-1";
